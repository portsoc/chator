# Ch@or API v1 (outdated)

There is one route:

* `/apiv1/messages`
  - GET: retrieve the last 50 messages, as JSON array of strings
  - POST: add a message, as a string with the content type `text/plain`
